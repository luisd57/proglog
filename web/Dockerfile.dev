FROM node:24-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
# --poll because file change events do not cross a Windows bind mount
CMD ["npm", "start", "--", "--host", "0.0.0.0", "--poll", "2000", "--proxy-config", "proxy.conf.json"]
